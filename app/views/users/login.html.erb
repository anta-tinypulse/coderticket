<div style='background-image: url(<%= image_path('background.png') %>)'>
  <div class="container text-center" style="padding-top: 120px">
    <h3 style="color: #FFF">Your Ticket to The Most Fabulous Event!</h3>

    <p style="color: #FFF">Login now to gain access to exclusive events in Vietnam!</p>

    <div class="row">
      <div class="col-sm-4"></div>

      <div class="signup-form col-sm-4">
        <%= form_for @user, url: sessions_path do |f| %>
            <div class="form-group" style="margin: 60px 0 30px 0">
              <%= f.text_field 'email', class: 'form-control', placeholder: 'Your E-mail' %>
            </div>
            <div class="form-group" style="margin: 30px 0 30px 0">
              <%= f.password_field 'password', class: 'form-control', placeholder: 'Password' %>
            </div>
            <div class="form-group" style="margin: 30px 0 30px 0">
              <%= f.submit 'Login', class: 'btn btn-block btn-info' %>
            </div>
        <% end %>
      </div>

      <div class="col-sm-4"></div>
    </div>

    <div class="additional-links">
      Haven't registered yet?<a href="<%= signup_path %>"> Sign up </a> here!
    </div>

    <div style="margin: 225px 0 20px 0; color: white; font-size: 12px">
      Coder Ticket &reg; 2015. All rights reserved.
    </div>
  </div>
</div>